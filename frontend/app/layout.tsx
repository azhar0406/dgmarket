import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { Providers } from '@/components/providers';
import { Toaster } from '@/components/ui/sonner';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'DG Market - Decentralized Gift Card Marketplace',
  description: 'Privacy-first decentralized gift card marketplace powered by Inco Lightning TEE and Chainlink Functions',
  keywords: 'decentralized, gift cards, marketplace, blockchain, privacy, TEE, Chainlink',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
          <Toaster />
        </Providers>
      </body>
    </html>
  );
}